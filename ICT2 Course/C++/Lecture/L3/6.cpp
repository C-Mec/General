#include <bits/stdc++.h>
using namespace std;
int c(pair<long long, long long> a, int i, int n) {
    // if (i-1 >= 0 && i+1 < n && a[i-1].first == )
}

int main() {
    int n, m, x;
    cin >> n >> m >> x;
    pair<long long, long long> a[n];
    for (int i = 0; i < n; i++) {
        cin >> a[i].first;
    }
    for (int i = 1; i < n - 1; i++) {
    }
}