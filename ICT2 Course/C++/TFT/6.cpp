#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, m;
    cin >> n >> m;
    long long t1, t2, t3;
    long long a[n];
    for (int i = 0; i < n; i++) {
        cin >> t1 >> t2 >> t3;
        a[i] = t1 + t2 + t3;
    }
    int max, maxid;
    for (int i = 0; i < m; i++) {
        max = 0;
        for (int j = 0; j < n; j++) {
            if (a[j] < m[x])
        }
    }
}